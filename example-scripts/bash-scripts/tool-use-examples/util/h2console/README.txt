H2Console example

Use the h2console.sh script to start up the h2 web server and a browser.  Once
the page is loaded make sure the following fields are set:

   Driver Class: org.h2.Driver
   JDBC URL: jdbc:h2:data/databaseclone/database</Param>
   User Name: sa
   Password:

Then select the Connect button.
